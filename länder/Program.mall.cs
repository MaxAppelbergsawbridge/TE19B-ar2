using System;
using System.IO;

namespace Countries
{
    class Program
    {
        static void Main(string[] args)
        {
            // Presentera programmet
            System.Console.WriteLine("Den här programmet söker upp länder åt dig");

            // Läs in alla rader från textfilen countries.csv
            foreach (var rad in rader)
            {
                string[] data = new string(rader); z
            }
        }
    }
}

